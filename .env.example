# Disabled by default. Existing in-browser extraction remains active.
VITE_QSS_REMOTE_PROCESSING=false

# Netlify server-only values. Never prefix secrets with VITE_.
QSS_PROCESSOR_URL=
QSS_PROCESSOR_TOKEN=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
VITE_GEMINI_REVIEW=false

